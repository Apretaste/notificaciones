<div class="row">
	<div class="col s12">
		<ul class="tabs tabs-fixed-width">
			<li class="tab"><a href="#" onclick="apretaste.send({command: 'NOTIFICACIONES ALERTS'})">Alertas</a></li>
			<li class="tab"><a href="#" class="active">Logs</a></li> 
		</ul>
	</div>
</div>

<div class="row">
	<div class="col s12">
	<% if(logs.length) { %>
			<ul class="collection">
				<% logs.forEach(function(item) { %>
					<li class="collection-item">
						<div>
							<span class="chip tiny clear grey-text text-darken-2"><i class="fas fa-calendar"></i> <%= moment(item.inserted).format('D [de] MMMM [del] YYYY, h:mm A') %></span>
							<br>
							<%= item.text %>
						</div>
					</li>
				<% }) %>
			</ul>
	<% } else { %>
		<div class="center-align">
			<h1>No hay logs</h1>
			<i class="material-icons large">notifications_off</i>
			<p>Nada importante ha ocurrido. Mientras use la app, registros de uso aparecerán acá.</p>
		</div>
	<% } %>
	</div>
</div>